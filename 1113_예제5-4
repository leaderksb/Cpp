#include <iostream>
#include "Circle.h"
using namespace std;

int main() {
	Circle circle;
	Circle &refc = circle;
	refc.setRadius(10);
	//circle.setRadius(10);
	cout << refc.getArea() << " " << circle.getArea() << endl;

	return 0;
}
